<Window
    x:Class="Sklad_2.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Přihlášení">

    <Grid Padding="24" Loaded="LoginWindow_Loaded">
        <StackPanel Spacing="12" VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="300">
            <TextBlock Text="Vyberte profil" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,24"/>
            
            <Button Content="Admin" HorizontalAlignment="Stretch" Style="{StaticResource PrimaryButtonStyle}" Command="{x:Bind ViewModel.SelectAdminCommand}"/>
            <Button Content="Prodej" HorizontalAlignment="Stretch" Style="{StaticResource PrimaryButtonStyle}" Command="{x:Bind ViewModel.SelectProdejCommand}"/>

            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Margin="0,24,0,0" Foreground="{ThemeResource SystemFillColorCriticalBrush}" TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
