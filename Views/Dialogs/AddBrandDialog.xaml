<ContentDialog
    x:Class="Sklad_2.Views.Dialogs.AddBrandDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Přidat značku"
    PrimaryButtonText="Uložit"
    CloseButtonText="Zrušit"
    DefaultButton="Primary"
    PrimaryButtonClick="PrimaryButton_Click">

    <StackPanel Spacing="12" MinWidth="400">
        <!-- Name -->
        <TextBox x:Name="NameTextBox"
                 Header="Název značky"
                 PlaceholderText="např. Maybelline, L'Oréal..."
                 TextChanged="TextBox_TextChanged"/>

        <!-- Description -->
        <TextBox x:Name="DescriptionTextBox"
                 Header="Popis (volitelné)"
                 PlaceholderText="např. Americká kosmetická značka..."
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="80"/>

        <!-- Error message -->
        <TextBlock x:Name="ErrorTextBlock"
                   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>
    </StackPanel>
</ContentDialog>
