<Page
    x:Name="page"
    x:Class="Sklad_2.Views.DatabazePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Sklad_2.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <TextBox
                PlaceholderText="Hledat podle názvu nebo EAN..."
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                MinWidth="300"/>
            <ComboBox
                Header="Kategorie"
                ItemsSource="{x:Bind ViewModel.Categories}"
                SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                MinWidth="150"/>
            <Button Command="{x:Bind ViewModel.DeleteProductCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74D;"/>
                    <TextBlock Text="Smazat vybrané"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ListView 
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.FilteredProducts, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedProduct, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.Header>
                <Grid Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="4*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="EAN" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Název ▲▼" Command="{x:Bind ViewModel.SortByCommand}" CommandParameter="Name" Style="{ThemeResource TextBlockButtonStyle}" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="2" Text="Kategorie" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Grid.Column="3" Content="Skladem ▲▼" Command="{x:Bind ViewModel.SortByCommand}" CommandParameter="StockQuantity" Style="{ThemeResource TextBlockButtonStyle}" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="4" Text="Sazba DPH" FontWeight="Bold" VerticalAlignment="Center" Visibility="{x:Bind ViewModel.IsVatPayer, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <TextBlock Grid.Column="5" Text="Nákupní cena" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Grid.Column="6" Content="Prodejní cena ▲▼" Command="{x:Bind ViewModel.SortByCommand}" CommandParameter="SalePrice" Style="{ThemeResource TextBlockButtonStyle}" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="7" Text="Sleva" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <Grid Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Ean}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Name}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind Category}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{x:Bind StockQuantity}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="{x:Bind VatRateFormatted, Mode=OneWay}" VerticalAlignment="Center" Visibility="{Binding ViewModel.IsVatPayer, ElementName=page, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <TextBlock Grid.Column="5" Text="{x:Bind PurchasePriceFormatted, Mode=OneWay}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="6" Orientation="Vertical" VerticalAlignment="Center">
                            <StackPanel>
                                <!-- Show original and discounted price -->
                                <TextBlock Visibility="{x:Bind IsDiscounted, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           Foreground="Gray"
                                           FontSize="11"
                                           HorizontalAlignment="Center">
                                    <Run Text="~~"/><Run Text="{x:Bind SalePriceFormatted, Mode=OneWay}"/><Run Text="~~"/>
                                </TextBlock>
                                <!-- Show final price (always visible) -->
                                <TextBlock Text="{x:Bind FinalPriceFormatted, Mode=OneWay}" 
                                           FontWeight="Bold"
                                           Foreground="{x:Bind IsDiscounted, Mode=OneWay, Converter={StaticResource BooleanToDiscountColorConverter}}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                        <Border Grid.Column="7" 
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="10"
                                Padding="6,2"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{x:Bind IsDiscounted, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{x:Bind DiscountPercentFormatted, Mode=OneWay}" 
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>