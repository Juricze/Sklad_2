<Page
    x:Class="Sklad_2.Views.ProdejPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Sklad_2.Models"
    xmlns:services="using:Sklad_2.Services"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Padding="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>

        <!-- Main content area -->
        <ScrollViewer Grid.Column="0" Margin="0,0,24,0" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24">
            <Border Style="{StaticResource CardBorderStyle}" Shadow="{ThemeResource CardShadow}">
                <TextBox x:Name="EanTextBox" Header="EAN kód produktu" PlaceholderText="Naskenujte nebo zadejte kód..." KeyDown="EanTextBox_KeyDown"/>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Shadow="{ThemeResource CardShadow}" Visibility="{x:Bind ViewModel.IsProductFound, Mode=OneWay}">
                <StackPanel Spacing="6">
                    <TextBlock Text="Poslední skenovaný produkt" Style="{ThemeResource TitleTextBlockStyle}"/>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Product Image -->
                        <Border Grid.Column="0"
                                Width="100" Height="100"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <Grid>
                                <!-- Placeholder icon -->
                                <FontIcon Glyph="&#xE91B;"
                                          FontSize="32"
                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Visibility="{x:Bind ViewModel.ScannedProduct.HasImage, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                                <!-- Product image -->
                                <Image Source="{x:Bind ViewModel.ScannedProductImage, Mode=OneWay}"
                                       Stretch="Uniform"
                                       Visibility="{x:Bind ViewModel.ScannedProduct.HasImage, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Border>

                        <!-- Product Details -->
                        <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                            <TextBlock><Run Text="Název:" FontWeight="Bold"/> <Run Text="{x:Bind ViewModel.ScannedProduct.Name, Mode=OneWay}"/></TextBlock>
                            <StackPanel>
                                <TextBlock>
                                    <Run Text="Cena:" FontWeight="Bold"/>
                                    <Run Text="{x:Bind ViewModel.ScannedProduct.FinalPriceFormatted, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Visibility="{x:Bind ViewModel.ScannedProduct.IsDiscounted, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           Foreground="{ThemeResource AccentFillColorDefaultBrush}">
                                    <Run Text="Původní cena:"/>
                                    <Run Text="{x:Bind ViewModel.ScannedProduct.SalePriceFormatted, Mode=OneWay}"/>
                                    <Run Text=" • Sleva"/>
                                    <Run Text="{x:Bind ViewModel.ScannedProduct.DiscountPercentFormatted, Mode=OneWay}"/>
                                    <Run Text=" ("/>
                                    <Run Text="{x:Bind ViewModel.ScannedProduct.DiscountReason, Mode=OneWay}"/>
                                    <Run Text=")"/>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock><Run Text="Skladem:" FontWeight="Bold"/> <Run Text="{x:Bind ViewModel.ScannedProduct.StockQuantity, Mode=OneWay}"/> <Run Text="ks"/></TextBlock>
                            <TextBlock Text="{x:Bind ViewModel.ScannedProduct.Description, Mode=OneWay}"
                                       Visibility="{x:Bind ViewModel.ScannedProduct.Description, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"
                                       MaxLines="2"
                                       TextTrimming="CharacterEllipsis"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Gift Card Redemption Section -->
            <Border Style="{StaticResource CardBorderStyle}" Shadow="{ThemeResource CardShadow}">
                <StackPanel Spacing="12">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Uplatnit dárkový poukaz" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock Text="💡 Pro platbu poukazem (ne prodej poukazu) - skenujte poukaz do tohoto pole, ne do pole pro produkty výše!"
                                   FontSize="12"
                                   Opacity="0.7"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Input for Gift Card EAN -->
                    <StackPanel Spacing="8">
                        <TextBox x:Name="GiftCardEanTextBox"
                                 Header="EAN kód poukazu k uplatnění"
                                 PlaceholderText="Naskenujte nebo zadejte EAN poukazu..."
                                 Text="{x:Bind ViewModel.GiftCardEanInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 KeyDown="GiftCardEanTextBox_KeyDown"/>
                        <Button Content="Načíst poukaz"
                                Click="LoadGiftCardButton_Click"
                                HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <!-- Loaded Gift Cards (ListView) -->
                    <Border Background="#E8F5E9"
                            BorderBrush="#4CAF50"
                            BorderThickness="2"
                            CornerRadius="8"
                            Padding="12"
                            Visibility="{x:Bind ViewModel.IsAnyGiftCardReady, Mode=OneWay}">
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8A1;" Foreground="#4CAF50" FontSize="20"/>
                                <TextBlock Text="Načtené poukazy pro platbu" FontWeight="SemiBold" Foreground="#4CAF50" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- ListView with loaded gift cards -->
                            <ListView ItemsSource="{x:Bind ViewModel.RedeemedGiftCards, Mode=OneWay}"
                                      SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:GiftCard">
                                        <Border Background="White"
                                                BorderBrush="#DDDDDD"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Padding="8"
                                                Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Spacing="2">
                                                    <TextBlock FontWeight="Bold">
                                                        <Run Text="Hodnota:"/>
                                                        <Run Text="{x:Bind ValueFormatted}"/>
                                                    </TextBlock>
                                                    <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                        <Run Text="EAN:"/>
                                                        <Run Text="{x:Bind Ean}"/>
                                                    </TextBlock>
                                                </StackPanel>

                                                <Button Grid.Column="1"
                                                        Content="Odstranit"
                                                        FontSize="12"
                                                        Padding="8,4"
                                                        VerticalAlignment="Center"
                                                        Click="RemoveGiftCard_Click"
                                                        Tag="{x:Bind Ean}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <Button Content="Zrušit všechny poukazy"
                                    Command="{x:Bind ViewModel.ClearAllGiftCardsCommand}"
                                    HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- Warning: Partial Usage (Forfeiture) -->
                    <Border Background="#FFF3CD"
                            BorderBrush="#FF9500"
                            BorderThickness="2"
                            CornerRadius="8"
                            Padding="12"
                            Visibility="{x:Bind ViewModel.WillHavePartialUsage, Mode=OneWay}">
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7BA;" Foreground="#FF9500" FontSize="20"/>
                                <TextBlock Text="Upozornění: Částečné využití" FontWeight="SemiBold" Foreground="#FF9500" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock TextWrapping="Wrap">
                                <Run Text="Celková hodnota poukazů je vyšší než celková částka košíku."/>
                            </TextBlock>
                            <TextBlock TextWrapping="Wrap">
                                <Run Text="Zbývající částka"/>
                                <Run Text="{x:Bind ViewModel.ForfeitedAmountFormatted, Mode=OneWay}" FontWeight="Bold"/>
                                <Run Text=" propadne a nelze ji použít v budoucnu."/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Věrnostní program -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    Shadow="{ThemeResource CardShadow}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Věrnostní program"
                               FontWeight="SemiBold"
                               FontSize="16"/>

                    <!-- Input pro vyhledání/načtení člena -->
                    <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.IsLoyaltyCustomerLoaded, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}">
                        <AutoSuggestBox x:Name="LoyaltySearchBox"
                                        PlaceholderText="Jméno, email nebo EAN kartičky..."
                                        Text="{x:Bind ViewModel.LoyaltySearchInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{x:Bind ViewModel.LoyaltySearchResults, Mode=OneWay}"
                                        TextChanged="LoyaltySearchBox_TextChanged"
                                        SuggestionChosen="LoyaltySearchBox_SuggestionChosen"
                                        QuerySubmitted="LoyaltySearchBox_QuerySubmitted">
                            <AutoSuggestBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="10" Padding="4">
                                        <TextBlock Text="{Binding FullName}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Email}" Opacity="0.7" FontSize="12"/>
                                        <TextBlock Text="{Binding CardEanFormatted}" Opacity="0.5" FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </AutoSuggestBox.ItemTemplate>
                        </AutoSuggestBox>
                    </StackPanel>

                    <!-- Loaded Loyalty Customer Info -->
                    <Border Background="#E3F2FD"
                            BorderBrush="#2196F3"
                            BorderThickness="2"
                            CornerRadius="8"
                            Padding="12"
                            Visibility="{x:Bind ViewModel.IsLoyaltyCustomerLoaded, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Spacing="6">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE779;" Foreground="#2196F3" FontSize="20"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedLoyaltyCustomer.FullName, Mode=OneWay}"
                                           FontWeight="SemiBold" Foreground="#2196F3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock>
                                <Run Text="Email:"/>
                                <Run Text="{x:Bind ViewModel.SelectedLoyaltyCustomer.Email, Mode=OneWay}" FontSize="12"/>
                            </TextBlock>
                            <TextBlock Visibility="{x:Bind ViewModel.SelectedLoyaltyCustomer.DiscountPercent, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                                <Run Text="Sleva:"/>
                                <Run Text="{x:Bind ViewModel.SelectedLoyaltyCustomer.DiscountFormatted, Mode=OneWay}" FontWeight="Bold" Foreground="#4CAF50"/>
                            </TextBlock>
                            <Button Content="Zrušit načteného člena"
                                    Command="{x:Bind ViewModel.ClearLoyaltyCustomerCommand}"
                                    HorizontalAlignment="Stretch"
                                    Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Cancel Last Receipt Warning -->
            <Border Background="#FFF3CD"
                    BorderBrush="#FF9500"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="16"
                    Visibility="{x:Bind ViewModel.CanCancelLastReceipt, Mode=OneWay}"
                    Shadow="{ThemeResource CardShadow}">
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE7BA;" Foreground="#FF9500" FontSize="24"/>
                        <TextBlock Text="Poslední prodej můžete zrušit"
                                   FontWeight="SemiBold"
                                   FontSize="16"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock TextWrapping="Wrap">
                            <Run Text="Účtenka č."/>
                            <Run Text="{x:Bind ViewModel.LastCreatedReceipt.ReceiptId, Mode=OneWay}" FontWeight="Bold"/>
                            <Run Text=" • "/>
                            <Run Text="{x:Bind ViewModel.LastCreatedReceipt.TotalAmountFormatted, Mode=OneWay}" FontWeight="Bold"/>
                            <Run Text=" • "/>
                            <Run Text="{x:Bind ViewModel.LastCreatedReceipt.PaymentMethod, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Text="Produkty budou vráceny do skladu a částka odečtena z pokladny."
                                   FontSize="12"
                                   Foreground="#666666"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <Button Content="Zrušit poslední prodej"
                            Click="CancelLastReceiptButton_Click"
                            Background="#FF3B30"
                            Foreground="White"
                            HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Receipt area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Shadow="{ThemeResource CardShadow}">
                <Grid RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Účtenka" Style="{ThemeResource TitleTextBlockStyle}"/>
                    <ListView Grid.Row="1"
                              ItemsSource="{x:Bind ViewModel.Receipt.Items, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedReceiptItem, Mode=TwoWay}"
                              SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="services:CartItem">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Spacing="2">
                                        <TextBlock Text="{x:Bind Product.Name}" FontWeight="Bold"/>
                                        <StackPanel>
                                            <TextBlock>
                                                <Run Text="{x:Bind QuantityFormatted, Mode=OneWay}"/>
                                                <Run Text=" x "/>
                                                <Run Text="{x:Bind UnitPrice, Mode=OneWay, Converter={StaticResource CurrencyConverter}}"/>
                                            </TextBlock>
                                            <!-- Show discount info if product has discount -->
                                            <StackPanel Visibility="{x:Bind HasDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                        Spacing="2">
                                                <TextBlock FontSize="12" 
                                                           Foreground="{ThemeResource AccentFillColorDefaultBrush}">
                                                    <Run Text="Původně:" FontStyle="Italic"/>
                                                    <Run Text="{x:Bind OriginalUnitPrice, Mode=OneWay, Converter={StaticResource CurrencyConverter}}"/>
                                                </TextBlock>
                                                
                                                <!-- Product discount (automatic) -->
                                                <TextBlock Visibility="{x:Bind HasProductDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                           FontSize="11" 
                                                           Foreground="#4CAF50">
                                                    <Run Text="📦 Sleva produktu: " FontWeight="SemiBold"/>
                                                    <Run Text="{x:Bind ProductDiscountDisplay, Mode=OneWay}"/>
                                                </TextBlock>
                                                
                                                <!-- Manual discount (additional) -->
                                                <TextBlock Visibility="{x:Bind HasManualDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                           FontSize="11" 
                                                           Foreground="#FF9500">
                                                    <Run Text="✋ Dodatečná sleva: " FontWeight="SemiBold"/>
                                                    <Run Text="{x:Bind ManualDiscountDisplay, Mode=OneWay}"/>
                                                </TextBlock>
                                                
                                                <!-- Combined discount total -->
                                                <TextBlock Visibility="{x:Bind HasDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                           FontSize="11" 
                                                           Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                                           FontWeight="SemiBold">
                                                    <Run Text="💰 "/>
                                                    <Run Text="{x:Bind CombinedDiscountDisplay, Mode=OneWay}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{x:Bind TotalPriceFormatted, Mode=OneWay}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <!-- Buttons for selected item -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                        <Button Command="{x:Bind ViewModel.IncrementQuantityCommand}" ToolTipService.ToolTip="Zvýšit množství vybrané položky">
                            <FontIcon Glyph="&#xE710;"/>
                        </Button>
                        <Button Command="{x:Bind ViewModel.DecrementQuantityCommand}" ToolTipService.ToolTip="Snížit množství vybrané položky">
                            <FontIcon Glyph="&#xE738;"/>
                        </Button>
                        <Button Command="{x:Bind ViewModel.ApplyManualDiscountCommand}" ToolTipService.ToolTip="Aplikovat slevu na vybranou položku" Background="{ThemeResource AccentFillColorDefaultBrush}" Foreground="White">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon Glyph="&#xE7B8;" FontSize="14"/>
                                <TextBlock Text="Sleva" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{x:Bind ViewModel.RemoveItemCommand}" ToolTipService.ToolTip="Odebrat vybranou položku">
                            <FontIcon Glyph="&#xE74D;"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Spacing="12" Margin="0,24,0,0">
                <StackPanel Spacing="4" HorizontalAlignment="Right">
                    <!-- Mezisoučet (jen pokud jsou slevy) -->
                    <Grid Visibility="{x:Bind ViewModel.HasAnyDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Mezisoučet:" Margin="0,0,8,0" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.GrandTotalFormatted, Mode=OneWay}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Grid>

                    <!-- Věrnostní sleva -->
                    <Grid Visibility="{x:Bind ViewModel.IsLoyaltyCustomerLoaded, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Věrnostní sleva:" Margin="0,0,8,0" FontSize="14" Foreground="#2196F3" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.LoyaltyDiscountAmountFormatted, Mode=OneWay}" FontSize="14" Foreground="#2196F3" FontWeight="SemiBold"/>
                    </Grid>

                    <!-- Poukazy -->
                    <Grid Visibility="{x:Bind ViewModel.IsAnyGiftCardReady, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Poukazy:" Margin="0,0,8,0" FontSize="14" Foreground="#4CAF50" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" FontSize="14" Foreground="#4CAF50" FontWeight="SemiBold">
                            <Run Text="-"/><Run Text="{x:Bind ViewModel.TotalGiftCardValueFormatted, Mode=OneWay}"/>
                        </TextBlock>
                    </Grid>

                    <!-- Separator pokud jsou slevy -->
                    <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,4"
                            Visibility="{x:Bind ViewModel.HasAnyDiscount, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- Přesná částka -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Celkem (přesně):" Margin="0,0,8,0" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.AmountToPayFormatted, Mode=OneWay}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Grid>

                    <!-- Zaokrouhlení (jen pokud není 0) -->
                    <Grid Visibility="{x:Bind ViewModel.HasRounding, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Zaokrouhlení:" Margin="0,0,8,0" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.RoundingDifferenceFormatted, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                    </Grid>

                    <!-- K ÚHRADĚ - finální zaokrouhlená částka -->
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="K ÚHRADĚ:" Margin="0,0,8,0" FontSize="20" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.AmountToPayRoundedFormatted, Mode=OneWay}" FontSize="20" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
                <Button Content="K Platbě (F1)" Click="CheckoutButton_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Stretch" ToolTipService.ToolTip="Stiskněte F1 pro rychlé přechod k platbě">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="F1" />
                    </Button.KeyboardAccelerators>
                </Button>
                <Button Content="Smazat účtenku" Click="ClearReceiptButton_Click" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>